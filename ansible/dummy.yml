- name: Dummy playbook
  hosts: proxmox
  become: yes
  gather_facts: yes
  tasks:
    - name: Create dummy file under /var/lib
      copy:
        dest: /var/lib/dummy.txt
        content: |
          This is a dummy file created by the dummy Ansible playbook.
          It exists only to verify connectivity over WireGuard.
        owner: root
        group: root
        mode: "0644"
