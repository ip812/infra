---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pgadmin-ingress
spec:
  defaultBackend:
    service:
      name: pgadmin-svc
      port:
        name: https
  ingressClassName: tailscale
