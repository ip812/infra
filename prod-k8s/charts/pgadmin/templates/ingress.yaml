---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pgadmin-ingress
  namespace: databases
  annotations:
    tailscale.com/hostname: pgadmin
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: pgadmin-svc
      port:
        number: 8080
  rules:
    - host: pgadmin.ip812.taile31a9d.ts.net
